@model Project3_DatingSite.Models.ForgotUsernameViewModel

@addTagHelper *, Microsoft.AspNetCore.Mvc.TagHelpers

@{
    Layout = null;
    ViewData["Title"] = "MatchRight - Forgot Username";
}

<!DOCTYPE html>
<html>
<head>
    <title>@ViewData["Title"]</title>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />

    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css" />
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.3.1/jquery.min.js"></script>
    <script src="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/js/bootstrap.min.js"></script>

    <style>
        body {
        background: linear-gradient(135deg, #f5e6d3, #e8d5b7);
        min-height: 100vh;
        font-family: Arial, Helvetica, sans-serif;
        }

        .page-wrapper {
        min-height: 100vh;
        display: flex;
        align-items: center;
        justify-content: center;
        padding: 30px 15px;
        }

        .page-card {
        background-color: #ffffff;
        border-radius: 16px;
        box-shadow: 0 8px 30px rgba(0, 0, 0, 0.15);
        overflow: hidden;
        max-width: 1000px;
        width: 100%;
        }

        .brand-side {
        background: linear-gradient(135deg, #1e90ff, #0066cc);
        color: white;
        padding: 50px 40px;
        min-height: 560px;
        }

        .brand-subtitle {
        font-size: 17px;
        margin-top: 20px;
        line-height: 1.7;
        }

        .feature-list {
        margin-top: 30px;
        padding-left: 20px;
        }

        .feature-list li {
        margin-bottom: 12px;
        font-size: 15px;
        }

        .form-side {
        padding: 45px 40px;
        background-color: #fffdf8;
        min-height: 560px;
        }

        .form-title {
        font-size: 30px;
        font-weight: bold;
        color: #0066cc;
        margin-bottom: 10px;
        }

        .form-subtitle {
        color: #666;
        margin-bottom: 25px;
        }

        .form-control {
        height: 46px;
        border-radius: 10px;
        border: 1px solid #ddd;
        box-shadow: none;
        }

        .form-control:focus {
        border-color: #1e90ff;
        box-shadow: 0 0 6px rgba(30, 144, 255, 0.3);
        }

        .btn-main {
        background-color: #1e90ff;
        border-color: #1e90ff;
        color: white;
        width: 100%;
        height: 46px;
        border-radius: 10px;
        font-weight: bold;
        }

        .btn-main:hover {
        background-color: #0066cc;
        border-color: #0066cc;
        color: white;
        }

        .link-group {
        margin-top: 20px;
        }

        .link-group a {
        display: block;
        margin-bottom: 10px;
        color: #0066cc;
        font-weight: 600;
        text-decoration: none;
        }

        .link-group a:hover {
        text-decoration: underline;
        }

        .message-box {
        margin-top: 15px;
        padding: 10px 12px;
        border-radius: 8px;
        background-color: #f5e6d3;
        color: #8b5e3c;
        font-weight: 600;
        }

        .result-box {
        margin-top: 15px;
        padding: 14px;
        border-radius: 8px;
        background-color: #eef6ff;
        border: 1px solid #c8def8;
        color: #004b8d;
        font-size: 16px;
        }

        @@media (max-width: 767px) {
        .brand-side {
        min-height: auto;
        text-align: center;
        padding: 35px 25px;
        }

        .form-side {
        min-height: auto;
        padding: 35px 25px;
        }
        }
    </style>
</head>
<body>
    <div class="page-wrapper">
        <div class="page-card">
            <div class="row" style="margin:0;">
                <div class="col-sm-5 brand-side">
                    <div style="text-align:center; margin-bottom:25px;">
                        <img src="~/images/matchright-logo.png"
                             alt="MatchRight Logo"
                             style="max-width: 240px; width: 100%;" />
                    </div>

                    <h3>Recover your MatchRight account.</h3>

                    <p class="brand-subtitle">
                        Enter the email tied to your account and we will help you recover your username.
                    </p>

                    <ul class="feature-list">
                        <li>Secure account recovery</li>
                        <li>Simple and quick lookup</li>
                        <li>Get back to connecting faster</li>
                    </ul>
                </div>

                <div class="col-sm-7 form-side">
                    <div class="form-title">Forgot Username</div>
                    <div class="form-subtitle">Enter your account email to retrieve your username.</div>

                    <form asp-controller="Account" asp-action="ForgotUsername" method="POST">
                        <div class="form-group">
                            <label asp-for="Email"></label>
                            <input asp-for="Email" class="form-control" placeholder="Enter your email address" />
                        </div>

                        <div class="form-group">
                            <input type="submit" value="Find Username" class="btn btn-main" />
                        </div>
                    </form>

                    @if (ViewData["Message"] != null)
                    {
                        <div class="message-box">
                            @ViewData["Message"]
                        </div>
                    }

                    @if (Model.RecoveredUsername != null && Model.RecoveredUsername != "")
                    {
                        <div class="result-box">
                            <strong>Your username is:</strong> @Model.RecoveredUsername
                        </div>
                    }

                    <div class="link-group">
                        <a asp-controller="Account" asp-action="Login">Back to Login</a>
                    </div>
                </div>
            </div>
        </div>
    </div>
</body>
</html>